{
  "name": "smallstash-web",
  "version": "0.1.0",
  "private": true,
  "description": "smallStash PWA client - all vault crypto happens here, in the browser, and nowhere else. See docs/architecture.md §3.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --experimental-test-module-mocks --test --test-isolation=none src/lib/**/*.test.js",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "amazon-cognito-identity-js": "^6.3.20",
    "fflate": "^0.8.3",
    "hash-wasm": "^4.12.0",
    "idb": "^8.0.3"
  },
  "devDependencies": {
    "@noble/hashes": "^2.4.0",
    "@playwright/test": "^1.63.0",
    "@sveltejs/vite-plugin-svelte": "^7.3.0",
    "fake-indexeddb": "^6.2.5",
    "svelte": "^5.57.0",
    "vite": "^8.3.0",
    "vite-plugin-pwa": "^1.3.0"
  }
}
